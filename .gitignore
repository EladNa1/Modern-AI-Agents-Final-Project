# OS / editor
.DS_Store
Thumbs.db
desktop.ini
~$*.pptx

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Env / secrets (keep .env.example committed)
.env
.env*.local
DEPLOY_HANDOFF.md
LECTURER_SIM_PROMPT.md
LECTURER_REPORT.md

# Node / Next.js
node_modules/
.next/
next-env.d.ts
.vercel
*.tsbuildinfo

# eval harness generated reports
eval/reports/
eval/ocr_cache/

# Internal planning docs — local only, not for GitHub
plans/
PLAN_*.md

# Claude Code local files
.claude/
CLAUDE.md

# QA / grader-simulation artifacts (local only)
evidence/
grading-evidence-*/
grading_run/
grading_evidence*/

# Demo-day deck + slide-probe scripts — local only, moved out of the repo (2026-08-22).
# Nothing in the pipeline imports them; they only bloated the clone.
presentation/
*.pptx

# Live-capture audit evidence (raw HTTP responses from probe runs) — local only
.audit_*/
