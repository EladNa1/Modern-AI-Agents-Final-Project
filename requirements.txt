# CheckMate — pure-Python runtime (FastAPI on Vercel).
fastapi>=0.115
uvicorn>=0.30
jinja2>=3.1
python-multipart>=0.0.9   # FastAPI multipart form uploads (/api/execute)
openai>=2.38              # LLMod.ai gateway (OpenAI-compatible): chat + embeddings
pinecone>=9.0             # vector RAG
pymupdf>=1.26             # PDF render + text-layer extraction (fitz)
